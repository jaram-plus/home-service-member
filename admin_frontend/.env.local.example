# Local development environment variables for admin_frontend
# NOTE: When using Docker, the root .env file is used instead.

# API Configuration
API_BASE_URL=http://localhost:8000
ADMIN_API_KEY=dev-admin-key-change-in-production

# TOTP Configuration
# Generate your own secret using:
# python -c "import secrets, base64; print(base64.b32encode(secrets.token_bytes(20)).decode())"
ADMIN_TOTP_SECRET=REPLACE_ME
TOTP_DEBUG=1
